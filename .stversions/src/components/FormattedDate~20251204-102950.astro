---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class='text-zinc-500 dark:text-zinc-400 tabular-nums'>
	{
		date.toLocaleDateString('en-us', {
			month: 'short',
			day: 'numeric',
			timeZone: 'Australia/Adelaide'
		})
	}
</time>
